-- DDL gerada a partir de data/imdb/movie_cast.parquet (1,580,459 linhas)
CREATE TABLE movie_cast (
    "IMDBID"     VARCHAR,
    "ORDERING"   DOUBLE,
    "NCONST"     VARCHAR,
    "CATEGORY"   VARCHAR,
    "JOB"        VARCHAR,
    "CHARACTERS" VARCHAR
);
