-- DDL gerada a partir de data/ancine/ancine_roe.parquet (13,401 linhas)
CREATE TABLE ancine_roe (
    "ROE"             VARCHAR,
    "TITULO_ORIGINAL" VARCHAR,
    "DIRETOR"         VARCHAR,
    "imdbID"          VARCHAR,
    "Title"           VARCHAR,
    "Director"        VARCHAR
);
